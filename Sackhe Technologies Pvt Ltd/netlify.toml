[build]
  command = "npm run build"
  publish = "dist"  # Change this to match your framework's output directory

[build.environment]
  NODE_VERSION = "18"  # Or your preferred version

# For single-page applications (React, Vue, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200